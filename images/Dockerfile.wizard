FROM buildpack-deps:trixie

ENV DEBIAN_FRONTEND=noninteractive \
    TZ=UTC \
    PNPM_HOME=/root/.local/share/pnpm \
    PATH=/root/.local/share/pnpm:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

RUN --mount=type=cache,target=/var/cache/apt \
    apt-get update && \
    apt-get install -y --no-install-recommends \
        ca-certificates \
        jq \
        libsqlite3-dev \
        python3 \
        python3-pip \
        sqlite3 \
        curl \
        git

RUN mkdir -p /etc/apt/keyrings && \
    curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg && \
    printf "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_20.x nodistro main\n" > /etc/apt/sources.list.d/nodesource.list && \
    apt-get update && \
    apt-get install -y --no-install-recommends nodejs && \
    rm -rf /var/lib/apt/lists/*

RUN corepack enable && \
    corepack prepare pnpm@9.12.2 --activate && \
    npm install -g @openai/codex@0.53.0

RUN groupadd -g 3000 codex && \
    useradd -m -u 3000 -g codex codex && \
    mkdir -p /home/codex/.codex && \
    chown -R codex:codex /home/codex

USER codex
WORKDIR /home/codex

ENV PNPM_HOME=/home/codex/.local/share/pnpm \
    PATH=/home/codex/.local/share/pnpm:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

ENTRYPOINT ["codex"]
